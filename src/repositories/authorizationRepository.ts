import {LoginInputType} from "../types/db.types";

export const authrizationRepository = {
    async authorization(authorizationData:LoginInputType){
        const auth  = null
        return auth
    }
}